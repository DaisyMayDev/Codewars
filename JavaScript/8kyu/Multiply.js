// Multiply:
// This code does not execute properly. Try to figure out why.

const a = 3;
const b = 7;

function multiply(a, b){
  return a * b;
}

console.log(`${a} x ${b} = ${multiply(a, b)}`);

//Output in console ->  3 x 7 = 21
