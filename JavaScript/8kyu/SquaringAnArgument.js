//Squaring an Argument:
//You have to write a function that takes an argument and returns the square of it.

const num = 3;

function square(num) {
  return num * num;
}

console.log(`${num} squared is ${square(num)}`);

//Output to Console ->  3 squared is 9
