// Make Uppercase:
// Write a function which converts the input string to uppercase.

const str = `hello`;

function makeUpperCase(str) {
  return str.toUpperCase();
}

console.log(`${str} to upper case is ${makeUpperCase(str)}`);

//Output to Console ->  hello to uppercase is HELLO
